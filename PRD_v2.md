# [PRD v2] 게임 로컬라이징 자동화 툴

> **v2 변경 이력**: PRD 검토에서 식별된 Critical 4건 + Major 6건 + Minor 일부를 반영한 수정본입니다.
> 변경된 부분은 `[v2 수정]` 태그로 표기합니다.

---

## [AI 코더(Developer Agent)를 위한 사전 지시사항]

당신은 이 PRD를 완벽하게 숙지하고, 요구사항을 100% 반영한 파이썬 웹 어플리케이션 코드를 작성해야 합니다. 본 툴은 코딩을 모르는 비개발자(기획, QA 직군)가 브라우저에서 버튼 클릭만으로 사용할 수 있도록 터미널(콘솔) 화면 없이 오직 Streamlit UI 내에서 모든 조작과 피드백이 이루어져야 합니다.

---

## 1. 프로젝트 개요 (Project Overview)

### 1.1. 프로젝트 목적
구글 스프레드시트 기반의 게임 텍스트(한국어)를 AI(Grok 4.1 Fast Reasoning)를 활용해 다국어(현재 EN, JA)로 자동 번역 및 검수하는 내부 툴 제작.

### 1.2. 핵심 과제
- 게임 클라이언트 크래시를 유발하는 포맷팅 태그(색상, 줄바꿈, 변수 등) 에러를 정규식으로 원천 차단.
- 고유명사 일관성 유지 (Glossary 강제 적용).
- **[중요]** 데이터 덮어쓰기 전 변경 사항(Diff) 문서화 및 2단계 Human-in-the-loop(사람의 승인) 과정을 거쳐 휴먼 에러 완벽 방지.
- 잦은 API 호출로 인한 구글 시트 Rate Limit 문제 해결 (Batch Read/Update).

### 1.3. 기술 스택
- Python
- Streamlit (Frontend)
- LangGraph (Agent Orchestration)
- gspread (Google Sheets API)
- LiteLLM (Grok 4.1 Fast Reasoning 호출용)
- Pandas (데이터/리포트 처리용)

### 1.4. `[v2 추가]` 배포 환경
- **Streamlit Cloud** 배포를 전제로 설계.
- Streamlit Cloud 제약 사항:
  - 앱 슬립/재부팅 시 **로컬 파일 시스템이 초기화**됨.
  - 따라서 백업 파일은 로컬 저장 + 즉시 다운로드 버튼 제공 방식으로 구현.
  - 영구 상태 저장이 필요한 경우 외부 스토리지(Google Drive, DB 등) 연동 고려.

---

## 2. 게임 세계관 및 로컬라이징 정책 (Lore & Guidelines)

> ⚠️ **개발자 주의**: 아래의 세계관과 톤앤매너는 번역 및 가이드라인 에이전트의 프롬프트에 SystemMessage 형태로 반드시 동적/정적 주입되어야 합니다.

### 2.1. 게임 시놉시스 (Context)
상자를 열기 전까지는 내용물을 알 수 없는 우주 최고 가챠 '슈뢰딩거 상자'로 대박을 터뜨린 우주 기업 카마존. 말단 배달 로봇 404가 배달 사고로 모든 상자를 잃어버리는데, 그 중 하나가 카마존 설립자가 주문한 전 우주에 하나 남은 단종된 한정판 '츄르'였다. 설립자의 전시 프로토콜 가동으로 외딴 행성에 불시착한 로봇 404의 츄르 찾기 대모험. (외계인들은 상자에 중독되어 카마존 상자를 닥치는 대로 탐내는 상황)

### 2.2. 번역 톤앤매너
너무 진지하지 않은 **유머러스(Humorous)**하고 **캐주얼(Casual)**한 톤을 철저히 유지.

### 2.3. 고유명사 고정 규칙 (Strict Glossary)

#### 일본어(JA) 등급 명칭 — 무조건 고정, 절대 의역 금지
Reviewer Agent가 반드시 검증 및 강제 치환합니다.

| 한국어 | 일본어(JA) |
|---|---|
| 일반 | 一般 |
| 고급 | 高級 |
| 희귀 | レア |
| 영웅 | 英雄 |
| 전설 | 伝説 |
| 신화 | 神話 |
| 고대 | 古代 |

#### `[v2 추가]` 영어(EN) 등급 명칭 — AI 자유 번역
영어 등급명은 고정 매핑을 적용하지 않으며, Translator Agent의 판단에 위임합니다. 단, **시트 전체에서 동일한 등급에 대해 일관된 번역이 유지**되어야 하며, Reviewer Agent가 일관성을 검증합니다.

### 2.4. 확장성 고려
- 현재 타겟 언어는 EN, JA 두 개이나, 추후 Streamlit UI에서 언어를 추가하면 시스템이 유연하게 확장되도록 모듈화된 코드로 작성할 것.
- `[v2 추가]` Glossary는 **언어별 딕셔너리 구조**로 관리하여, 추후 언어 추가 시 해당 언어의 Glossary만 추가하면 되도록 설계.

```python
# Glossary 구조 예시
GLOSSARY = {
    "ja": {
        "일반": "一般",
        "고급": "高級",
        "희귀": "レア",
        "영웅": "英雄",
        "전설": "伝説",
        "신화": "神話",
        "고대": "古代",
    },
    # "en": {},  # EN은 현재 AI 자유 번역
    # "zh": {},  # 추후 확장 시 추가
}
```

---

## 3. 구글 시트 데이터 구조 관리

### 3.1. 스프레드시트 접근 규칙 (Strict Constraints)

**접근 및 노출 절대 금지 시트** (코드 단에서 필터링하여 UI 드롭다운에 노출 금지):
- `사용법`, `Texture`, `수정금지_Common`

**작업 대상 시트**:
- `Common`, `Stat`, `Skill`, `SkillEffect`, `Tactic`, `Story`, `Unit`, `Item`, `Box`, `Product`, `Dialogue`, `Etc` 등.

### 3.2. 필수 컬럼 구조 및 Tool_Status 제어

시트는 `Key`, `Shared Comments`, `Korean(ko)`, `English(en)`, `Japanese(ja)` 컬럼을 가집니다.

`[v2 수정]` **컬럼 탐색 방식**: 컬럼 인덱스(위치)가 아닌 **헤더 이름(텍스트) 기반**으로 컬럼을 탐색합니다. 이를 통해 컬럼 순서 변경이나 추가 컬럼이 있어도 정상 작동합니다.

`[v2 수정]` **Shared Comments 활용**: 이 컬럼의 내용이 비어있지 않은 경우, **Translator Agent에 해당 행의 번역 컨텍스트(참고 정보)로 주입**합니다.

**[중요] 제어 컬럼 자동 생성 로직**: 툴이 선택된 시트를 최초로 로드할 때, 맨 우측에 `Tool_Status` 라는 컬럼이 존재하는지 파이썬 코드로 확인하고, 없다면 자동으로 해당 컬럼을 생성해야 합니다.

**상태값(Status)**:

| 상태 | 설명 |
|---|---|
| `대기` | 초기 상태 또는 작업 미시작 |
| `한국어검수완료` | Step 1 한국어 검수 승인 완료 |
| `번역중` | Step 2 번역/검수 파이프라인 진행 중 |
| `검수실패` | `[v2 추가]` Reviewer 3회 재시도 후에도 실패한 행 |
| `최종완료` | Step 3 최종 승인 후 시트 업데이트 완료 |

각 행별로 상태를 기록하여, 중간에 서버가 끊기더라도 재개(Resume)가 가능하게 설계해야 합니다.

---

## 4. 핵심 기능 요구사항 (Core Workflows)

### 4.1. 작업 모드 선택
사용자는 UI에서 다음 두 가지 모드 중 하나를 선택하여 작업을 시작합니다.

- **모드 A (전체 번역/검수)**: 기존 번역 데이터(en, ja) 유무와 상관없이 Korean(ko) 열을 기준으로 타겟 언어 전체를 재번역. (리뉴얼 및 재검수용, 4.4항의 변경사항 승인 절차 필수)
- **모드 B (빈칸 번역)**: 타겟 언어 열이 비어있는(Blank) 행(Row)만 필터링하여 번역을 수행. (API 비용 절감 및 신규 업데이트용)

### 4.2. Step 1: 한국어(ko) 사전 검수 및 문서화 (HITL 1)

> ⚠️ **개발자 주의**: LangGraph 파이프라인의 첫 번째 일시 정지(`interrupt()`) 구간입니다.

1. AI가 작업 대상 행들의 Korean(ko) 원문을 읽고 맞춤법, 띄어쓰기, 단어 일관성을 검사합니다.
2. 기존 원문과 AI의 수정 제안이 다른 경우, 이를 모아 **[한국어 변경 사항 리포트 (CSV/Excel)]** 문서를 생성하고 Streamlit UI에 다운로드 버튼과 함께 데이터프레임을 표출합니다.
3. 사용자가 문서를 검토 후 화면에서:
   - **[👍 변경 사항 승인 및 번역 시작]** 또는
   - **[👎 제안 무시하고 원본 그대로 번역 시작]** 버튼을 클릭합니다.
4. 승인된 텍스트를 기준으로 `Tool_Status`를 `한국어검수완료`로 변경하고 Step 2로 상태를 전이합니다.

### 4.3. Step 2: 다중 에이전트 파이프라인 (LangGraph 5 Nodes)

`[v2 수정]` Node 1, Node 2는 LLM 호출 없이 파이썬 유틸리티 함수로 구현합니다 (비용 절감).

| Node | 이름 | LLM 사용 | 역할 |
|---|---|---|---|
| Node 1 | Data & Backup Agent | **No** | 타겟 시트의 데이터를 읽어오고 로컬에 원본 백업본을 생성. **백업 CSV 즉시 다운로드 버튼 제공.** |
| Node 2 | Context & Glossary Agent | **No** | 2장의 시놉시스, 톤앤매너, Glossary를 셋업하여 번역 가이드라인을 구성. |
| Node 3 | Translator Agent | **Yes** | 가이드 + `Shared Comments` + 한국어 원문을 바탕으로 타겟 언어(EN, JA) 번역을 수행. |
| Node 4 | Reviewer Agent | **Yes** | 번역 결과물을 원본과 엄격하게 크로스체크(정규식 태그 검증 + Glossary 검증 + AI 품질 검증). 에러 시 Node 3으로 피드백을 주어 **재시도(최대 3회)**. |
| Node 5 | Writer Agent | **No** | 4.4항의 최종 승인이 떨어지기 전까지 시트에 접근하지 않고 대기. 승인된 데이터만 시트에 일괄(Batch) 업데이트. |

`[v2 추가]` **3회 재시도 실패 시 처리**: Node 4에서 Node 3으로 3회 피드백 후에도 태그/Glossary 검증을 통과하지 못하면:
1. 해당 행의 `Tool_Status`를 `검수실패`로 마킹
2. 실패 사유를 별도 리포트에 기록
3. 다음 행으로 진행하여 전체 프로세스가 중단되지 않도록 함
4. 최종 리포트에 실패 행 목록을 포함하여 사용자에게 안내

### 4.4. Step 3: 최종 번역본 변경 사항 검토 및 승인 (HITL 2 - 가장 중요)

> ⚠️ **개발자 주의**: Node 4(Reviewer) 작업 완료 후, Node 5(Writer)가 시트를 덮어쓰기 직전에 발생하는 두 번째 일시 정지(`interrupt()`) 구간입니다. 이 장치가 없으면 개발팀 데이터가 손상될 수 있습니다.

1. 특히 '모드 A (전체 번역)' 진행 시, **기존 구글 시트에 있던 번역(EN/JA)과 AI가 새로 번역/검수한 결과물이 달라진 부분(Diff)**만 추출합니다.
2. 이 비교 데이터를 **[번역 변경 사항 리포트 (CSV)]** 문서로 묶어 Streamlit UI에 다운로드 버튼과 함께 제공합니다.
3. 표 형태:

| Key | 언어 | 기존 번역 | 새 번역 | 변경 사유/내역 |
|---|---|---|---|---|
| UNIT_001 | EN | Old Robot | New Robot | `[v2 추가]` Reviewer Agent가 AI로 자동 생성 |

4. 개발자나 QA가 해당 문서를 확인한 후, UI에서 **[🚀 시트에 최종 업데이트 적용]** 버튼을 클릭해야만 Node 5 (Writer Agent)가 작동하여 구글 시트를 덮어씁니다.
5. 반려 시 업데이트를 취소합니다.

### 4.5. 구글 API 제한 방지 (Batch Read & Update)

`[v2 수정]` Read와 Write 모두 Batch 전략을 적용합니다.

**Write 전략**:
- gspread 사용 시, `for`문 안에서 `cell.update()` 개별 호출을 **엄격히 금지**.
- 반드시 파이썬 리스트/딕셔너리에 결과값을 모아두었다가 `worksheet.update(range_name, values)`를 통해 한 번에 덮어쓰는(Batch Update) 방식으로 구현.

**`[v2 추가]` Read 전략**:
- Google Sheets API는 **분당 Read 300회** 제한이 있음.
- `worksheet.get_all_records()` 또는 `worksheet.get_all_values()`를 **1회 호출**하여 전체 데이터를 Pandas DataFrame에 로드.
- 이후 모든 데이터 조회/필터링/비교는 **메모리(DataFrame) 내에서** 수행.
- 시트 재조회가 필요한 경우 **exponential backoff** 재시도 로직 적용.

### `[v2 추가]` 4.6. 배치 처리 단위
- LLM API 호출은 **청크(Chunk) 단위**(10~20행)로 배치 처리합니다.
- 각 청크 처리 완료 시 Progress Bar를 업데이트합니다.
- 청크 크기는 토큰 제한과 컨텍스트 효율성을 고려하여 조정합니다.

---

## 5. 사용자 인터페이스 (UI/UX - Claude Style)

> ⚠️ **개발자 주의**: `.streamlit/config.toml` 설정과 `st.markdown` 커스텀 CSS 주입을 통해, 비개발자가 편안함을 느낄 수 있는 "Warm & Minimalist" 스타일을 반드시 구현해야 합니다.

### 5.1. 전체 무드 (Vibe)
따뜻함, 미니멀리즘, 부드러운 둥근 모서리(`border-radius: 12px ~ 16px`), 옅은 그림자(Box Shadow).

### 5.2. 컬러 팔레트
| 용도 | 컬러 |
|---|---|
| 메인 배경 | `#F6F5F0` |
| 카드 배경 | `#FFFFFF` |
| 포인트 컬러 | `#C85A32` |
| 텍스트 | `#2C2C2C` |

### 5.3. 화면 레이아웃 구조

**사이드바**:
- 구글 시트 URL 입력
- 작업 대상 시트 선택 (필터링 필수 — 3.1항의 금지 시트 제외)
- 타겟 언어 선택
- 모드 선택 (A: 전체 번역, B: 빈칸 번역)
- [작업 시작] 버튼

**메인 대시보드**:
- 봇 초대 안내 문구 표출
- **[문서 다운로드 영역]**: Step 1 및 Step 3에서 발생하는 '변경 사항 리포트(CSV)'를 다운받을 수 있는 명시적인 버튼 배치. `[v2 추가]` 백업 CSV 다운로드 버튼도 포함.
- **[승인 액션 영역]**: 확인/반려를 결정하는 눈에 띄는 버튼 배치.
- 진행 상황 Progress Bar 및 실시간 로그 모니터(`st.expander` 활용 스크롤 형태).
- `[v2 추가]` 작업 완료 후 **사용된 토큰 수 / 예상 비용** 요약 표시.

---

## 6. 데이터 보안 및 백업 (Security & Backup)

### 6.1. 권한 인증
`st.secrets`에 저장된 Google Service Account JSON 키를 통해 시트에 접근합니다.
UI 상단에 다음 안내 문구를 렌더링하세요:

> 💡 [공유] 버튼을 누르고 아래 봇 이메일을 **편집자**로 초대해 주세요: `[봇 이메일]`

### 6.2. 강제 백업 로직
`[v2 수정]` 사용자가 '작업 시작'을 누르면 어떠한 프로세스보다 먼저 `[시트명]_backup_YYYYMMDD_HHMM.csv` 형태로 원본을 백업해야 합니다.

**Streamlit Cloud 대응**:
- 백업 파일을 로컬(`/tmp/` 또는 앱 디렉토리)에 저장한 후, **즉시 `st.download_button`으로 다운로드 버튼을 UI에 제공**.
- `st.session_state`에도 DataFrame을 보관하여 세션 내에서는 접근 가능하도록 유지.
- 사용자에게 "백업 파일을 반드시 다운로드하세요" 안내 메시지 표시.

---

## 7. 개발자 참고 및 필수 제약 사항 (Developer Notes)

### 7.1. LangGraph State(상태) 객체 및 체크포인터

`[v2 수정]` 에이전트 간 순환 및 **2번의 Interrupt(HITL)**를 지원하도록 State 클래스와 체크포인터를 세밀히 정의하세요.

**State 클래스 예시 필드**:
```python
class LocalizationState(TypedDict):
    sheet_name: str
    mode: str  # "A" or "B"
    target_languages: list[str]

    # 데이터
    original_data: list[dict]      # 원본 시트 데이터
    backup_data: list[dict]        # 백업용
    ko_review_results: list[dict]  # 한국어 검수 결과
    translation_results: list[dict]  # 번역 결과
    review_results: list[dict]     # 검수 결과
    failed_rows: list[dict]        # 검수실패 행 목록

    # 리포트
    diff_report_ko: Optional[str]           # 한국어 변경 리포트 (CSV)
    diff_report_translation: Optional[str]  # 번역 변경 리포트 (CSV)

    # HITL 제어
    wait_for_ko_approval: bool       # HITL 1 대기
    ko_approval_result: Optional[str]  # "approved" or "rejected"
    wait_for_final_approval: bool    # HITL 2 대기
    final_approval_result: Optional[str]

    # 진행 상태
    current_chunk_index: int
    total_chunks: int
    retry_count: dict  # {row_key: retry_count}

    # 비용 추적
    total_input_tokens: int
    total_output_tokens: int
```

**`[v2 추가]` 체크포인터 전략**:
```python
from langgraph.checkpoint.memory import MemorySaver

# Streamlit Cloud에서는 MemorySaver 사용 (세션 내 유지)
checkpointer = MemorySaver()
graph = workflow.compile(checkpointer=checkpointer)

# st.session_state에 thread_id 저장
if "thread_id" not in st.session_state:
    st.session_state.thread_id = str(uuid.uuid4())

config = {"configurable": {"thread_id": st.session_state.thread_id}}
```

**`[v2 추가]` HITL Interrupt/Resume 패턴**:
```python
from langgraph.types import interrupt, Command

# Node 내에서 HITL 중단
def ko_review_node(state):
    # ... 한국어 검수 수행 ...
    approval = interrupt({"report": diff_report, "type": "ko_review"})
    # 사용자 승인 후 여기서 재개
    return {"ko_approval_result": approval}

# Streamlit에서 Resume
if st.button("👍 승인"):
    graph.invoke(Command(resume="approved"), config=config)
```

### 7.2. `[v2 추가]` asyncio 이벤트 루프 충돌 해결

Streamlit은 자체 이벤트 루프를 운영하므로, LangGraph와 충돌이 발생할 수 있습니다.

**해결 전략**: LangGraph **동기 API**를 우선 사용합니다.
```python
# 동기 호출 (권장)
result = graph.invoke(initial_state, config=config)

# 비동기가 반드시 필요한 경우 nest_asyncio 적용
import nest_asyncio
nest_asyncio.apply()
```

### 7.3. Reviewer Node의 정규식(Regex) 강제 검증 로직 (매우 중요)

LLM 프롬프트에만 의존하지 말고 **파이썬 `re` 모듈을 사용해 하드코딩된 검증 로직**을 포함하세요.

원본 Korean(ko)에 존재하는 아래 패턴의 **개수와 문자열이 번역 결과물(en, ja)에 동일하게 존재하는지** `re.findall()`로 철저히 비교할 것.

`[v2 수정]` 확장된 검증 패턴 목록:
```python
TAG_PATTERNS = [
    r'\{[^}]+\}',           # 변수 태그: {player_name}, {0}, {1}
    r'%[dsf]',              # printf 스타일 변수: %d, %s, %f
    r'<color[^>]*>',        # 색상 시작 태그: <color=#FF0000>
    r'</color>',            # 색상 종료 태그
    r'</?b>',               # 볼드 태그
    r'</?i>',               # 이탤릭 태그
    r'<size=[^>]*>',        # 사이즈 태그
    r'</size>',             # 사이즈 종료 태그
    r'\\n',                 # 줄바꿈
    r'\\t',                 # 탭
]
```

> **참고**: 실제 시트 데이터에서 추가로 사용 중인 태그가 발견되면 이 목록에 추가해야 합니다. 개발 초기 단계에서 시트 데이터를 스캔하여 사용 중인 태그 패턴을 사전 조사하세요.

### 7.4. 일본어 등급 후처리(Post-processing) 강제 적용

일본어 번역 결과물은 매핑 딕셔너리를 활용해 파이썬 `replace()` 함수 등으로 한 번 더 **강제 치환 및 검증**할 것.

### 7.5. LLM API 클라이언트 설정 (Grok 4.1 Fast Reasoning 연동)

`[v2 수정]` 정확한 모델 설정:

```python
import litellm

# .env 또는 st.secrets에서 API 키 로드
XAI_API_KEY = st.secrets["XAI_API_KEY"]

# LiteLLM을 통한 Grok 4.1 Fast Reasoning 호출
response = litellm.completion(
    model="xai/grok-4-1-fast-reasoning",
    api_key=XAI_API_KEY,
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": user_prompt}
    ],
)
```

| 항목 | 값 |
|---|---|
| 모델 ID | `grok-4-1-fast-reasoning` |
| LiteLLM 모델 ID | `xai/grok-4-1-fast-reasoning` |
| 엔드포인트 | `https://api.x.ai/v1` |
| API 키 환경변수 | `XAI_API_KEY` |
| 컨텍스트 윈도우 | 2,000,000 토큰 |
| Rate Limit | 4M TPM / 480 RPM |
| Input 가격 | $0.20 / 1M tokens |
| Output 가격 | $0.50 / 1M tokens |
| Cached Input 가격 | $0.05 / 1M tokens |

---

## 부록: v1 → v2 변경 사항 전체 목록

| # | 구분 | 섹션 | 변경 내용 |
|---|---|---|---|
| 1 | Critical | 7.5 | 모델 ID `grok-beta` → `grok-4-1-fast-reasoning` |
| 2 | Critical | 7.1 | LangGraph 체크포인터(`MemorySaver`) + `st.session_state` 연동 전략 추가 |
| 3 | Critical | 7.2 | asyncio 이벤트 루프 충돌 해결 전략(동기 API 우선) 추가 |
| 4 | Critical | 1.4, 6.2 | Streamlit Cloud 배포 환경 제약 반영 (파일 시스템, 백업 전략) |
| 5 | Major | 4.3 | 3회 재시도 실패 시 `검수실패` 마킹 + 다음 행 진행 로직 추가 |
| 6 | Major | 3.2 | Shared Comments 컬럼 → Translator Agent 컨텍스트로 활용 명시 |
| 7 | Major | 3.2 | 컬럼 탐색을 헤더 이름 기반으로 변경 |
| 8 | Major | 4.3 | Node 1, 2를 LLM 미사용 유틸리티 함수로 명확화 |
| 9 | Major | 4.5 | Google Sheets Read 전략 (1회 전체 로드 + DataFrame) 추가 |
| 10 | Major | 4.4 | Diff 리포트 변경 사유를 Reviewer Agent AI 자동 생성으로 명시 |
| 11 | Minor | 2.3 | EN 등급명 AI 자유 번역 + 일관성 검증 정책 추가 |
| 12 | Minor | 2.4 | Glossary 언어별 딕셔너리 구조 설계 추가 |
| 13 | Minor | 4.6 | 배치 처리 단위(청크) 정의 추가 |
| 14 | Minor | 7.3 | 정규식 검증 패턴 목록 확장 |
| 15 | Minor | 5.3 | 토큰 사용량/비용 요약 UI 추가 |
